# Communicative Interaction Log — Session 13

**Date:** 2026-04-14
**Session ID:** `2026-04-14-session-02`
**Participants:** James (한지훈) ↔ Claude (Opus 4.6, 1M context)
**Scope:** CP-3 UI self-proving system implementation (6 features) + systematic interaction audit + broken link fixes + 3-axis tooltip

---

## 1. Starting State

- **Last commit:** `aaac6a2` (Phase 0: YAML frontmatter for 296 atoms)
- **Phase 0 complete:** 296 atoms enriched with YAML frontmatter (persons, organizations, fracture-type, record-nos, tags)
- **CP-3 plan approved:** 6 items agreed in session-01, awaiting implementation
- **graph.json/detail.json:** Regenerated with new fields from Phase 0

---

## 2. Major Decisions

### 2.1 CP-3 implementation order: Landing → Layer Narrative → Guided Proof → others

Implemented in dependency order: Landing metrics (independent) → Layer Narrative (modifies existing layer header) → Guided Proof (most complex, depends on GraphDataService methods) → Record No. / Person / Fracture (incremental, use Phase 0 fields).

### 2.2 Full interaction audit after CP-3

James identified specific broken links (chain view Record No., detail "체인 보기"/"상세" links) and requested systematic audit. An Opus-level Explore subagent was dispatched to audit ALL click handlers across 4 components. Found 7 broken interactions (3 HIGH, 2 MEDIUM, 2 LOW).

### 2.3 3-axis tooltip as accessibility requirement

James requested hover explanations for 진리성/타당성/진실성 badges. These are Beyond-Security framework terms that external visitors cannot understand without context. Implemented as bilingual `[title]` attribute tooltips — native browser behavior, no library dependency.

### 2.4 Dead code identified but not deleted

`query-input` component (unused), `searchResults`/`chainMode` unused `@Input()` properties identified. Decision: leave in place due to parallel session risk (another session may have added these intentionally).

---

## 3. Phase Progress

### CP-3: Self-Proving System UI (6/6 COMPLETE)

| # | Feature | Implementation |
|---|---|---|
| 3.1 | Landing 증명화 | 4 metrics (입증률/발견사항/균열/증거기록) + guided entry button + layer tower corroboration ratio |
| 3.2 | Guided Proof Mode | `getStrongestFractures(7)` → cross-layer argumentative sequence with falsification conditions, fracture badges, Record No. inline links |
| 3.3 | Layer Narrative | `getLayerCrossLinks()` → ← L1 / → L6 arrows + `getLayerFractures()` → top 3 key fractures per layer |
| 3.4 | Person Proof Path | `getPersonIndex()` / `getAtomsByPerson()` → clickable persons in right panel with layer dots, `personSelect` event |
| 3.5 | Record No. activation | `getAtomsByRecordNo()` → reverse-index overlay, `onRecordClick()` handler |
| 3.6 | Fracture subtype | Color-coded badges: F-SC(red)/F-CE(orange)/F-MS(blue)/F-SE(teal)/F-AA(pink) |

### Interaction Audit + Fixes (7 issues found, 6 fixed)

| # | Severity | Issue | Fix |
|---|---|---|---|
| 1 | HIGH | Chain view Record No. cursor:pointer but no click | Added `(click)="onRecordClick(r)"` |
| 2 | HIGH | Answer view Record No. stopPropagation only | Connected `onRecordClick(r)` |
| 3 | HIGH | Chat history click doesn't re-search | `onHistoryClick` → delegates to `onSearch` |
| 4 | MEDIUM | Evidence-context Record No. not clickable | Added `recordSelect` event + clickable styling |
| 5 | MEDIUM | Answer "all records" table not clickable | Added `record-link` class + click |
| 6 | ADDED | 3-axis badges no explanation | Bilingual hover tooltip + cursor:help |
| 7 | LOW | Dead code (query-input, unused inputs) | Identified, deferred |

---

## 4. Still Pending

### Phase D (배포) — next major milestone
1. 가명화 3중 방어 (pseudonym guard: build-time scan + runtime filter + CSP)
2. `ng build --configuration production` → static bundle
3. GitHub Pages or Vercel deployment
4. OG metatags for social sharing
5. Legal disclaimer (면책 고지)

### Operational
- `git push` — 11 local commits unpushed (growing risk)
- Obsidian Dataview rendering visual confirmation (S7 from Phase 0)

---

## 5. Open Methodological Questions

1. **Phase D 진입 시점:** CP-3 완료 + 인터랙션 수정 완료. Phase D 승인 필요.
2. **가명화 3중 방어 설계:** build-time Python scanner + Angular runtime guard + Content Security Policy — 설계 검토 필요.

---

## 6. Key Files Touched

| File | Description |
|---|---|
| `ui/src/app/services/graph-data.service.ts` | +176 lines: getPersonIndex, getAtomsByPerson, getAtomsByRecordNo, getStrongestFractures, getLayerCrossLinks, getLayerFractures, enhanced getStats |
| `ui/src/app/components/landing/*` | CP-3.1: proof metrics section, guided proof button, corrPct in layer tower |
| `ui/src/app/components/proof-body/*` | CP-3.2+3.3: guided proof view, layer narrative, Record No. click fixes, 3-axis tooltips |
| `ui/src/app/components/proof-shell/*` | Guided proof wiring, person/record select handlers, chat history fix |
| `ui/src/app/components/evidence-context/*` | Person clickable with layer dots, Record No. clickable, recordSelect output |
| `ui/src/app/pipes/as-any.pipe.ts` | NEW: utility pipe for template type casting |
| `ui/angular.json` | Component style budget 8kB→32kB |

---

## 7. Token Economics

- **CP-3 implementation:** ~60k tokens (6 features across 5 components)
- **Interaction audit (Opus subagent):** ~30k tokens (comprehensive, found 7 issues)
- **Fixes:** ~15k tokens (6 targeted edits + build verification)
- **Session total:** ~105k tokens
- **Builds:** 3 successful Angular builds (verification after each major batch)
